
class Graph {
    adjacencyList:{[x:string]:Array<string>}
    constructor(public x:number) {
        this.adjacencyList = {};
    }

    addVertex(vertex:string):void {
        if(!(vertex in this.adjacencyList)) {
            this.adjacencyList;
            this.x
        }
    }
}